# frozen_string_literal: true

RSpec.describe Kanal::Plugins::UserSystem do
  it "has a version number" do
    expect(Kanal::Plugins::UserSystem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
